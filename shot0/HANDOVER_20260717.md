# Shot0 引き継ぎ書(2026-07-17 11:35頃 / Code=クラウドセッションより)

宛: 次のCodeセッション(ローカル推奨)・Claude・Codex / 発: Code(クラウド、Claude窓経由で円卓投稿)

## 現在の状態(この瞬間)

- **実機(take-MacPro)でkernel buildが進行中**: `nohup bash shot0/scripts/build_kernel_shot0.sh > ~/shot0_build.log 2>&1`
  - 11時前の時点で `grep -c '^  CC' ~/shot0_build.log` = 3,883(目安2万で終盤)。nice -19なので数時間コース
  - 完了するとログ末尾に `linux-image-*+shot0*.deb` / `linux-headers-*+shot0*.deb` のSHA256一覧が出る
- **実機の設定は何一つ変えていない**(build=userland作業のみ。install・GRUB・配線すべて未実施)
- **Codexは円卓の署名境界不備(09:09報告)の修理中**でstop-switch ON。Shot0監査要請は順番待ち
- 王の方針: 「Codexの作業が終わったら確認」「Shot0がダメなら諦める」

## 完了済み(証拠付き)

| 項目 | 証拠 |
|---|---|
| Shot0実装一式(quirk+scripts+test) | branch `claude/work-session-yoz077` commit `30c7d35` |
| build事前チェック全数化 | commit `cd6d814` |
| 模擬試験T1〜T8 | サンドボックス+実機の両方で全PASS |
| 静的検証V1〜V5(実ソースlinux-7.0.0) | 全PASS。V1: pci_setup_device内 fixup_early(行69)<pci_read_bases(行97) |
| 監査用diff | `~/shot0_build/linux-7.0.0/shot0_evidence/shot0_quirks.diff` SHA256=0888711aab6d4fe0cd30d1badfdabfab596b6af48ec81764e4ec40399b3477c7 |
| 円卓報告 | 09:20実装完了 / 10:06静的検証全PASS(共にClaude名義・署名Code) |
| build開始指示書(王裁可の記録) | `proposals/Claude提案_20260717-0929_Shot0_build開始指示書_王裁可済み_20260717.md` |

## 次にやること(順番厳守)

1. **build完了確認**: `tail -15 ~/shot0_build.log` にdeb SHA256が出ているか。エラー停止していたら出力を読んで対処(事前チェックはcd6d814で全数化済みなので、以降のエラーはmake本体)
2. **円卓へbuild完了報告**: deb SHA256・所要時間・bake_meter水位(build中に荒れていないか)を添えて
3. **Codex監査待ち**: 監査観点=source diff・fixup相・config write順・対象限定・package/GRUB/rollback・凍結露出。材料は全て円卓報告済み
4. **監査合格後**: 王へ直前報告 → `sudo bash shot0/scripts/arm_oneshot_grub.sh <image.deb> <headers.deb>`(SHOT0-ARM手入力ゲート付き。rebootはしない)
5. **実射**: 王の手で3060単体・右下口・cold boot 1回 → 起動後 `bash shot0/scripts/collect_logs_shot0.sh` → 円卓報告
6. **結果をfindings.mdへ**: 成否どちらでも記録(live resize凍結10回の夜戦も未記載なら併せて)

## 禁止事項(王命・継続)

- live resize / remove / rescan / `pci=assign-busses` / 2枚同時cold boot
- kernel適用・GRUB予約・再起動・配線変更は**王へ直前報告してから**。引き金は常に王
- 3060接続は王の同席時のみ

## 申し送り(クラウドセッションの反省)

- クラウドCodeは実機シェルに届かない(Tailscale網外)。王にコマンドを貼らせる往復が発生した — **次はローカルセッション推奨**
- 円卓監視のsend_laterトリガー(30分間隔)はこのセッション終了と共に切れる。ローカル側で見張りを引き継ぐこと
- 王への説明は例え話が有効だった(カーネル=大家さん、BAR=部屋の畳数)
- 失敗time-line: flex/bison不足→deb-src無効→libdw-dev不足の3連停止。事前チェックは全数化済みだが、resolute(26.04)のkernel package再ビルドにはまだ地雷があり得る

## 追記(11:30頃・離脱直前)

- build失敗1回あり: **gawk不足**(modules.builtin.ranges生成、log 24323行目)。コンパイル本体は完走済みで、失敗はvmlinuxリンク前段
- 対処: 事前チェックへ gawk/zstd/dwarves を追加(commit `e455f48`)。実機で `sudo apt-get install -y gawk zstd dwarves` → `git pull` → build再実行(再開は続きからなので15〜40分見込み)
- 最後の再実行はsudoパスワード入力と`&`が干渉した可能性あり。実機で `jobs` と `tail -3 ~/shot0_build.log` で走っているか要確認。走っていなければ: apt installを先に単独実行→buildをnohupで再起動
- 完成の合図: ログ末尾に `linux-image-*+shot0*.deb` のSHA256一覧

署名Code(クラウド・session yoz077・離脱済み)
