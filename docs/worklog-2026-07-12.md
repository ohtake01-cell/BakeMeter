# 作業記録: 2026-07-12 bake_meter v2.1 統合と復帰バグ修正 (Claude)

王命「報告書を見て、普通に作業を進めろ。作業記録を書け」による作業。
Codex はトークン制限で停止中のため、以下を設計図として単独で進めた:
- 窯側の申し送り「Code申し送り_化けメーター改修の契約_20260712.md」
  (PR #1 読了済みの移植チェックリスト+王裁定クールダウン30分の追記を含む)
- GitHub PR #1 (bake_meter v2.1, head=claude/moshi-moshi-atzbf1, cadf355)

## やったこと

1. **PR #1 の v2.1 スクリプトを現 main (a0e3fce) に整合させて導入**
   - PR #1 は分岐点が古く(c49ad68)、そのままマージすると日本語README(80行)と
     v2水位のREADME更新が消える。本ブランチは現 main の上に v2.1 を再構成した。
2. **復帰バグ(-1汚染)の修正** — 事故報告書の防止策5
   - 病根: フォールバック時に state へ `-1` を保存 → sysfs 復帰時に
     DELTA = 累積値+1 → 偽DANGER → 全モデル退避の誤爆
   - 修正(4手): ①読み込み時に非負整数のみ信用 ②フィールド欠けstateは破棄
     ③フォールバック時は最後に読めた本物の値を保持(-1はJSON表示のみ)
     ④state書き込みをアトミック化(書きかけ破損の根絶)
   - 設計の芯: **stateファイルには本物のカウンタ値以外を絶対に入れない**
3. **王裁定の反映**: クールダウン既定 60分→30分(12:51裁定・静けさ条件は維持)
4. **回帰テスト追加**: `scripts/test_bake_meter.sh`
   - 偽sysfsツリーで実スクリプトを回す13ケース(root/GPU/Ollama不要)
   - 基本状態機械(初回/WARN/DANGER/COOLDOWN/リブートリセット/冷却明け)
   - 事故再現系(脱落→復帰の偽DANGER、初日フォールバック、復帰直後の本物
     バーストは見逃さない、state破損)
   - 修正版: 13/13 合格。旧v2.1(PR #1): 5ケース失敗 → テストがバグを捕まえる
     ことも確認済み
5. **README(日英)を v2.1 の実態へ更新**: COOLDOWN水位・30分・フォールバックが
   基準値を汚染しないこと・BM_*環境変数・テスト同梱を明記
6. **事故報告書の防止策5を「是正済み」へ更新**

## 互換契約の遵守 (申し送りの契約1・2)

- state 3フィールド形式(`total level last_danger`)と bake_state.json の
  フィールド名は無変更。`cooldown_until_epoch` も維持
- 王国固有の `last_danger_epoch`/`recent30_warn_or_worse`/PYSESSブロックは
  王国側 live の拡張であり、公開版には元々含まれない(移植時はCodexの担当領分)
- stale時 fail-closed の掟(0253)に沿ったコメントを維持(fail-open文言は不採用)

## Codex への申し送り

- 王国へ移植する際は、この統合版(claude/moshi-moshi-jvjxse)を起点にすると
  -1バグ修正込みで移植できる。BM_COOLDOWN_MIN 既定は30(王裁定)反映済み
- PR #1 をそのまま main へマージしないこと(日本語READMEが消える)。
  本ブランチが PR #1 の内容+整合+修正を包含している
- 移植後の互換確認は王国側ハーネス(test_bake_gate_v2.py 等)に加え、
  公開側 `scripts/test_bake_meter.sh` も王国 live に対して流用可能
